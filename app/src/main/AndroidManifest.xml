<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ekspeace.buddystaff">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuddyStaff">
        <activity android:name=".Activities.Clients" android:label=""/>
        <activity android:name=".Activities.Notification" android:label=""/>
        <activity android:name=".Activities.Price" android:label=""/>
        <activity android:name=".Activities.Database" android:label=""/>
        <activity android:name=".Activities.Password" android:label="" />
        <activity android:name=".Activities.Declined" android:label=""/>
        <activity android:name=".Activities.Accepted" android:label=""/>
        <activity android:name=".Activities.Pending" android:label=""/>
        <activity android:name=".Activities.Approve" android:label=""/>
        <activity android:name=".Activities.Dashboard" android:label="" />
        <activity android:name=".Activities.Splash"  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>